import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#81fd91',
      contrastText: '#110909',
    },
    secondary: {
      main: '#FB8C00',
    },
    background: {
      default: '#F8FAFC',
      paper: '#ffffff',
    },
  },
});
