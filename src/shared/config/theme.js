import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#081627',
      contrastText: '#fffbfb',
    },
    secondary: {
      main: '#133958',
      contrastText: '#35ad55',
    },
    background: {
      default: '#ffffff',
      paper: '#ffffff',
    },
  },
});
