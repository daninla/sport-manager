import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#081627',
      contrastText: '#fffbfb',
    },
    secondary: {
      main: '#133958',
      contrastText: '#c27f2e',
    },
    background: {
      default: '#ffffff',
      paper: '#ffffff',
    },
  },
});
