import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0A192F',
      contrastText: '#ffffff',
    },
    secondary: {
      main: '#FB8C00',
    },
    background: {
      default: '#F8FAFC',
      paper: '#ffffff',
    },
  },
});
